#include "main.h"
#include <stdio.h>

/**
 * mul - une fonction qui multiplie deux entiers.
 * @a: premiere number
 * @b: dexième number
 * Return: returne les entiers
 */

int mul(int a, int b)
{
	return (a * b);
}
